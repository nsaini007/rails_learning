<% unless comment.archived? %>
    <p>
        <strong><%= comment.commentor %></strong> : <%= comment.body %>
        <%= link_to "Destroy Comment",[comment.article,comment],data:{
            turbo_method: :delete,
            turbo_confirm: "Are you sure?"
        } %>
    </p>
<% end %>